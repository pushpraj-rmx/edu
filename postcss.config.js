// Tailwind CSS v4 is handled by @tailwindcss/vite plugin in vite.config.js
// No PostCSS configuration needed when using Vite plugin
// Tailwind v4 handles vendor prefixing automatically
export default {
    plugins: {},
};
